# FastS3 Rust workspace(数据面 + S3 协议层)
# 模块边界见 docs/DESIGN.md §13;依赖方向:fs3d 装配一切,禁止反向依赖
[workspace]
resolver = "2"
members = [
    "crates/fs3-core",
    "crates/fs3-device",
    "crates/fs3-alloc",
    "crates/fs3-meta",
    "crates/fs3-engine",
    "crates/fs3-s3",
    "crates/fs3-http",
    "crates/fs3-admin",
    "crates/fs3-agent",
    "crates/fs3d",
]

[workspace.package]
version = "2.3.0"
edition = "2021"
license = "Apache-2.0"
rust-version = "1.88"

[workspace.dependencies]
fs3-core = { path = "crates/fs3-core" }
fs3-device = { path = "crates/fs3-device" }
fs3-alloc = { path = "crates/fs3-alloc" }
fs3-meta = { path = "crates/fs3-meta" }
fs3-engine = { path = "crates/fs3-engine" }
fs3-s3 = { path = "crates/fs3-s3" }
fs3-http = { path = "crates/fs3-http" }
fs3-admin = { path = "crates/fs3-admin" }
fs3-agent = { path = "crates/fs3-agent" }

# —— 依赖最小化原则(ROADMAP §2 / 风险 R9):新增依赖须说明理由 ——
thiserror = "2.0"
libc = "0.2"
rocksdb = { version = "0.25", default-features = false }
serde = { version = "1.0", features = ["derive"] }
postcard = { version = "1.1", features = ["use-std"] }
io-uring = "0.7"
md-5 = "0.10"
quick-xml = "0.41"
hmac = "0.12"
parking_lot = "0.12"
sha2 = "0.10"
# sha1:M10 S4 POST 表单 SigV2 签名(HMAC-SHA1);已在依赖树(tokio-tungstenite
# 经 fs3-admin 引入 0.10.7),复用锁树版本,不引入新包。
sha1 = "0.10"
hex = "0.4"
base64 = "0.22"
# zstd:M13 Z1 数据压缩(写时压缩,默认关;zstd crate = 官方 rust 绑定,
# C 后端捆绑编译,零外部依赖;档位 1~3)。
zstd = "0.13"
serde_json = "1.0"
rcgen = "0.13"
clap = { version = "4.5", features = ["derive"] }
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
proptest = "1.5"
tempfile = "3"

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
