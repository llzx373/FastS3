[package]
name = "fs3-engine"
description = "FastS3 存储引擎:读写路径、extent、CRC、恢复"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
fs3-core.workspace = true
fs3-device.workspace = true
fs3-alloc.workspace = true
fs3-meta.workspace = true
io-uring.workspace = true
hex.workspace = true
libc.workspace = true
md-5.workspace = true
tracing.workspace = true
# M11 K1-1 SSE-S3:seed/DEK 局部副本用完即擦(ADR-12 DS1 红线;fs3-core
# 已引入同 crate 同大版本,非新增外部依赖)
zeroize = "1"
# M13 Z1 数据压缩(zstd crate;压缩=compression,区别于 compaction)
zstd.workspace = true
# M19 Batch(ADR-26 DR2.3):Inventory manifest.json 解析(仅 admin/worker
# 路径,非热路径)
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true
proptest.workspace = true
parking_lot.workspace = true
